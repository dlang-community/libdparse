module misc_coverage;

uint a[*!*];

const extern([Zzzz]) a = 8;

auto a(8) = 8;

void foo()
{
    foreach(a;z) break ++ZzZ--;
}

class Base : List, ++ZzZ--, Of, Base {}

void foo()
{
    switch(foo)
    {
        case low: .. case high ++ZzZ--;
        case arg,list ++ZzZ--;
        default ++ZzZ--;
    }
    switch(foo)
    {
        case ++
    }
    switch(foo)
    {
        case one: final a;
    }
    switch(foo)
    {
        case one: static a;
    }
}

enum E
{
    a,
    b = ++ZzZ[*!*]ZzZ--;
}

alias a = 0, b = a;

version ++ZzZ--;

void foo()
{
    for (i; cond(); i++) }
}

alias @++ const void null;

alias int fun() @++;

alias T = @++ U;

alias T = extern ++ZzZ-- U;

int[8] a = [+o+,+o+];

import ++;

__vector( a;
__vector) a;
__vector() a;
__vector(__vector) a;

class A : SomeDynarray[] {}

//keep at the end
void foo(){a +=
